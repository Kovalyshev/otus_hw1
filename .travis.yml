language: cpp
compiler:
- g++
env:
  global:
  - BUILD_NUMBER=*
script:
- cmake . -DBUILD_NUMBER=$TRAVIS_BUILD_NUMBER
- cmake --build .
- ./bin/unit_tests
- cmake --build . --target package
branches:
  only:
  - master
